# PAINEL-DO-LOJISTA

## Git hooks

Run the local hook bootstrap once after cloning:

```bash
make setup-hooks
```

After that, `git commit` will format staged files automatically before the commit is created.
